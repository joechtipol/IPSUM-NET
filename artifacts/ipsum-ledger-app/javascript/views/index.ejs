<% include partials/header.ejs %>
    <div class="table-wrapper">
        <% 
        var dcs=digitalCredentials.toString();
                
        var listRecords=JSON.parse(dcs);
        console.log(`Coco avant parsing result dcs: ${listRecords.length}`);
        

        if (digitalCredentials.length > 0) {%>
            <table class="table table-hovered">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">First Name</th>
                        <th scope="col">Last Name</th>
                        <th scope="col">Birth Date</th>
                        <th scope="col">Program name</th>
                        <th scope="col">Ipfs content</th>
                    </tr>
                </thead>
                <tbody>
                    <% 
                    
                    //digitalCredentials.forEach((dlCredential, index) => {
                    for (var entry of listRecords.entries()) {
                    var Index = entry[0],
       			 dlCredential = entry[1];
       				 console.log(`Coco mon ami result dlCredential: ${dlCredential}`);
       				 
                     %>
                        <tr>
                            <th scope="row"><%= dlCredential.Key %></th>
                            <td><%= dlCredential.Record.first_name %></td>
                            <td><%= dlCredential.Record.last_name %></td>
                            <td><%= dlCredential.Record.birth_date %></td>

                            <td><%= dlCredential.Record.program_name %></td>
                            <td>
                                <a href="https://ipfs.io/ipfs/<%= dlCredential.Record.hash_content %>" class="btn btn-sm btn-danger">View Digital credential</a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center">No Digital credentials found. Go <a href="/app/add" >here</a> to add DLs.</p>
        <% } %>
    </div>
</div>
</body>
</html>
